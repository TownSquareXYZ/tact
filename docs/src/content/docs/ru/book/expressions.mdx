---
title: Выражения
description: На этой странице перечислены все выражения в Tact
---

import { Badge } from '@astrojs/starlight/components';

Каждый оператор в Tact формирует выражение, но есть гораздо больше возможностей для раскрытия, поскольку Tact предлагает широкий спектр вариантов выражения на выбор.

:::note

  Текущий максимально допустимый уровень вложенности выражений составляет $83$. Попытка написать более глубокое выражение приведет к ошибке компиляции:

```tact
fun elegantWeaponsForCivilizedAge(): Int {
    return
    ((((((((((((((((((((((((((((((((
        ((((((((((((((((((((((((((((((((
            (((((((((((((((((((( // 84 скобки, ошибка компиляции!
                42
            ))))))))))))))))))))
        ))))))))))))))))))))))))))))))))
    ))))))))))))))))))))))))))))))));
}
```

:::

## Литералы

Литералы представляют значения в Tact. Это фиксированные значения, а не переменные, которые вы _буквально_ указываете в своем коде. Все литералы в Tact сами по себе являются выражениями.

Вы также можете вызывать [функции расширения](/book/functions#extension-function), определенные для некоторых [примитивных типов][p], соответствующих литералам, прямо для значений литералов:

```tact
// Вызов toString() определенной для Int, для целочисленного литерала:
42.toString();

// Вызов asComment(), определенной для String, для строкового литерала:
"Tact - это потрясающе!".asComment();
```

### Целочисленные литералы

Целочисленные литералы могут быть записаны в [десятичной](/book/integers#decimal) (основание $10$), [шестнадцатеричной](/book/integers#hexadecimal) (основание $16$), [восьмеричной](/book/integers#octal) (основание $8$) и [двоичной](/book/integers#binary) (основание $2$) системах счисления:

- Литерал [_десятичных_ целых](/book/integers#decimal) представляет собой последовательность цифр ($\mathrm{0 - 9}$).

- Начальный литерал $\mathrm{0x}$ (или $\mathrm{0X}$) указывает на [шестнадцатеричное целое число](/book/integers#hexadecimal). Он может включать цифры ($\mathrm{0 - 9}$) и буквы $\mathrm{a - f}$ и $\mathrm{A - F}$. Обратите внимание, что регистр символа не меняет его значение. Следовательно: $\mathrm{0xa}$ = $\mathrm{0xA}$ = $10$ и $\mathrm{0xf}$ = $\mathrm{0xF}$ = $15$.

- Начальный литерал $\mathrm{0o}$ (или $\mathrm{0O}$) указывает на [восьмеричное целое число](/book/integers#octal). Он может включать только цифры $\mathrm{0 - 7}$.

- Начальный литерал $\mathrm{0b}$ (или $\mathrm{0B}$) указывает на [двоичное целое число](/book/integers#binary). Он может включать только цифры $0$ и $1$.

:::caution
  Будьте осторожны, в Tact целочисленные литералы с ведущим $0$ по-прежнему считаются десятичными, в отличие от JavaScript/TypeScript, где ведущий $0$ указывает на восьмеричное число!
:::

Некоторые примеры целочисленных литералов:

```tact
// десятичное, основание 10:
0, 42, 1_000, 020

// шестнадцатеричное, основание 16:
0xABC, 0xF, 0x0011

// восьмеричное, основание 8:
0o777, 0o001

// двоичное, основание 2:
0b01111001_01101111_01110101_00100000_01100001_01110010_01100101_00100000_01100001_01110111_01100101_01110011_01101111_01101101_01100101
```

Подробнее о целых числах и типе [`Int{:tact}`](/book/integers) читайте на специальной странице: [целые числа](/book/integers).

### Логические литералы

Тип [`Bool{:tact}`](/book/types#booleans) имеет только два литеральных значения: `true{:tact}` и `false{:tact}`.

```tact
true == true;
true != false;
```

Подробнее о логических значениях и типе [`Bool{:tact}`](/book/types#booleans) читайте в специальной главе: [логические значения](/book/types#booleans).

### Строковые литералы

Строковый литерал — это ноль или более символов, заключенных в двойные (`"`) кавычки. Все строковые литералы являются объектами типа [`String{:tact}`][p].

```tact
"foo";
"1234";
```

Строки Tact поддерживают ряд [экранированных последовательностей](https://en.wikipedia.org/wiki/Escape_sequence), начинающихся с символа обратной косой черты `\\`:

- `\\\\{:tact}` - литеральная обратная косая черта
- `\"{:tact}` - двойная кавычка
- `\n{:tact}` - новая строка
- `\r{:tact}` - возврат каретки
- `\t{:tact}` - табуляция
- `\v{:tact}` - вертикальная табуляция
- `\b{:tact}` - возврат на одну позицию
- `\f{:tact}` - подача формы
- \x00{:tact}`через`\xFF{:tact}\` — [кодовая точка](https://en.wikipedia.org/wiki/Code_point), длина которой должна составлять ровно две шестнадцатеричные цифры
- `\u0000{:tact}` по `\uFFFF{:tact}` — [кодовая точка Unicode][unicode], длина которой должна составлять ровно четыре шестнадцатеричных знака
- `\u{0}{:tact}` по `\u{FFFFFF}{:tact}` — [кодовая точка Unicode][unicode], может содержать от $1$ до $6$ шестнадцатеричных цифр

[Юникод]: https://en.wikipedia.org/wiki/Unicode#Codespace_and_code_points

```tact
// \\
" \\ спасайся, \\ если \\ сможешь \\";

// \"
"это \"буквально\" работает";

// \n
"строка \n в другой строке";

// \r
"Ставки \r как \r эта \r одна";

// \t
"интервал \t предоставлен!";

// \v
"эти \v слова \v выровнены";

// \b
"rm\b\bcreate!";

// \f
"формируем \f подачу";

// \x00 - \xFF
"это \x22буквально\x22 работает"; // \x22 представляет двойную кавычку

// \u0000 - \uFFFF
"опасно, \u26A1 высокое напряжение \u26A1"; // \u26A1 представляет эмодзи ⚡

// \u{0} - \u{FFFFFF}
"\u{1F602} ЛОЛ \u{1F602}"; // \u{1F602} представляет эмодзи 😂
```

:::note

  Подробнее о строках и типе [`String{:tact}`][p]:\
  [Примитивные типы в книге][p]\
  [Строки и StringBuilders в справочнике](/ref/core-strings)

:::

### Литерал `null`

Значение `null{:tact}` записывается с помощью литерала `null{:tact}`. Это **не** [идентификатор](#identifiers) и не относится ни к какому объекту. Это также **не** экземпляр [примитивного типа][p]. Вместо этого `null{:tact}` представляет собой отсутствие идентификации и намеренное отсутствие какого-либо значения.

```tact
let var: Int? = null; // переменная, которая может содержать значение null
var = 42;
if (var != null) {
    var!! + var!!;
}
```

Подробнее о работе с `null{:tact}` читайте на специальной странице: [дополнительные параметры](/book/optionals).

## Идентификаторы

Идентификатор — это последовательность символов в коде, которая _идентифицирует_ [переменную](/book/statements#let), [константу](/book/constants), [карту](/book/maps) и [функцию](/book/functions), а также [строки][s], [сообщения][m], [контракты](/book/contracts), [типажи](/book/types#traits) или их поля и методы. Идентификаторы чувствительны к регистру и не заключаются в кавычки.

В Tact идентификаторы могут содержать строчные латинские буквы `a-z`, заглавные латинские буквы `A-Z`, подчеркивания `_` и цифры $\mathrm{0 - 9}$, но не могут начинаться с цифры. Никакие другие символы не допускаются, а идентификаторы Unicode запрещены.

Обратите внимание, когда идентификаторы для [примитивных типов][p] начинаются с заглавной буквы. Используемые [составные типы](/book/types#composite-types), такие как [структуры][s] и [сообщения][m], также должны быть написаны с заглавной буквы.

:::caution

  Все идентификаторы, начинающиеся с `__gen` и `__tact`, не допускаются и зарезервированы для внутреннего использования компилятором.

:::

## Создание экземпляра

Вы можете создавать экземпляры следующих типов:

- [Структуры][s]
- [Сообщения][m]

```tact
struct StExample {
    fieldInit: Int = 1;
    fieldUninit: Int;
}

fun example() {
    // Экземпляр со значением fieldInit по умолчанию
    StExample{ fieldUninit: 2 };

    // Экземпляр с обоими установленными полями
    StExample{
        fieldInit: 0,
        fieldUninit: 2, // разрешается запятая в конце
    };
}
```

## Доступ к полям

Вы можете напрямую обращаться к полям следующих типов:

- [Структуры][s]
- [Сообщения][m]

```tact
struct StExample {
    fieldInit: Int = 1;
    fieldUninit: Int;
}

fun example(): Int {
    let struct: StExample = StExample{ fieldUninit: 2 }; // создание экземпляра

    struct.fieldInit;          // доступ к полю
    return struct.fieldUninit; // возврат значения поля из функции
}
```

## Вызов функции расширения

[Функции расширения](/book/functions#extension-function) определяются только для конкретных типов. Их можно вызывать аналогично вызовам методов во многих других языках:

```tact
42.toString(); // toString() - это функция stdlib, определенная для типа Int
```

## Вызов статической функции

В любом месте тела функции может быть вызвана глобальная [статическая функция](/book/functions#global-static-functions) или внутренняя функция [контракта](/book/contracts):

```tact
contract ExampleContract {
    receive() {
        now(); // now() — это статическая функция stdlib
        let expiration: Int = now() + 1000; // операция и объявление переменной
        expiration = self.answerQuestion(); // внутренняя функция
    }
    fun answerQuestion(): Int {
        return 42;
    }
}
```

## `initOf`

<Badge text="Gas-expensive" title="Использует 500 единиц газа или более" variant="danger" size="medium"/><p/>

Выражение `initOf{:tact}` вычисляет начальное состояние, т. е. `StateInit{:tact}` [контракта](/book/contracts):

```tact
//                     значения аргументов для функции init() контракта
//                     ↓   ↓
initOf ExampleContract(42, 100); // возвращает Struct StateInit{}
//     ---------------
//     ↑
//     имя контракта
//     ↓
//     ---------------
initOf ExampleContract(
    42,  // первый аргумент
    100, // второй аргумент, допускается запятая в конце
);
```

`StateInit{:tact}` — это встроенная [структура][s], которая состоит из:

| Поле   | Тип                   | Описание                                                                                    |
| :----- | :-------------------- | :------------------------------------------------------------------------------------------ |
| `code` | [`Ген{:tact}`][cell]  | начальный код [контракта](/book/contracts) (скомпилированный байт-код)                      |
| `data` | [`Cell{:tact}`][cell] | начальные данные [контракта](/book/contracts) (аргументы функции `init(){:tact}` контракта) |

:::note

  Для воркчейна $0$ [`Address{:tact}`][p] текущего контракта, полученного путем вызова функции [`myAddress(){:tact}`](/ref/core-common#myaddress), идентичен тому, который можно получить путем вызова функции [`contractAddress(){:tact}`](/ref/core-common#contractaddress) с начальным состоянием текущего контракта, вычисленным с помощью `initOf{:tact}`:

```tact {6}
contract TheKingPrawn {
    receive("keeping the address") {
        let myAddr1 = myAddress();
        let myAddr2 = contractAddress(initOf TheKingPrawn());

        myAddr1 == myAddr2; // true
    }
}
```

  Однако, если вам нужен только адрес текущего контракта во время выполнения, а не его `StateInit{:tact}`, используйте функцию [`myAddress(){:tact}`](/ref/core-common#myaddress), так как она потребляет **значительно** меньше газа.

:::

[p]: /book/types#primitive-types
[клетка]: /book/cells#cells
[s]: /book/structs-and-messages#structs
[m]: /book/structs-and-messages#messages
